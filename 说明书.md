# PMPC 四遥数据管理系统 使用说明书

版本：2.4
日期：2026年6月
适用平台：Windows / Linux / macOS

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构](#2-系统架构)
3. [文件结构](#3-文件结构)
4. [配置说明](#4-配置说明)
   - [4.1 point_cfg.ini —— 四遥点表配置](#41-point_cfgini--四遥点表配置)
   - [4.2 app_modules.ini —— 模块主配置](#42-app_modulesini--模块主配置)
   - [4.3 modbus_tcp_master.ini —— Modbus TCP 主站采集配置](#43-modbus_cfgini--modbus-tcp-主站采集配置)
   - [4.4 modbus_tcp_slave.ini —— Modbus TCP 从站配置](#44-modbus_tcp_slaveini--modbus-tcp-从站配置)
   - [4.5 pemp_server.ini —— PEMP2.0 推送服务配置](#45-tcp_cfgini--pemp20-推送服务配置)
   - [4.6 debug_console.ini —— 调试控制台配置](#46-debug_consoleini--调试控制台配置)
   - [4.7 packet_logger.ini —— 报文记录器配置](#47-packet_loggerini--报文记录器配置)
   - [4.8 redundancy.ini —— 双机冗余配置](#48-redundancy_cfgini--双机冗余配置)
   - [4.9 data_recorder.ini —— MySQL 数据存储配置](#49-data_recorderini--mysql-数据存储配置)
   - [4.10 modbus_rtu_master.ini —— Modbus RTU 主站采集配置](#410-modbus_rtu_masterini--modbus-rtu-主站采集配置)
   - [4.11 modbus_rtu_slave.ini —— Modbus RTU 从站配置](#411-modbus_rtu_slaveini--modbus-rtu-从站配置)
   - [4.12 iec104_master.ini —— IEC 104 主站配置](#412-iec104_masterini--iec-104-主站配置)
   - [4.13 iec104_slave.ini —— IEC 104 从站配置](#413-iec104_slaveini--iec-104-从站配置)
   - [4.14 iec103_master.ini —— IEC 103 主站配置](#414-iec103_masterini--iec-103-主站配置)
   - [4.15 iec101_master.ini —— IEC 101 主站配置](#415-iec101_masterini--iec-101-主站配置)
   - [4.16 iec101_slave.ini —— IEC 101 从站配置](#416-iec101_slaveini--iec-101-从站配置)
   - [4.17 dlt645_master.ini —— DLT 645 电表配置](#417-dlt645_masterini--dlt-645-电表配置)
   - [4.18 cdt_master.ini —— CDT 主站配置](#418-cdt_masterini--cdt-主站配置)
   - [4.19 cdt_slave.ini —— CDT 从站配置](#419-cdt_slaveini--cdt-从站配置) —— Modbus RTU 主站采集配置](#410-modbus_rtuini--modbus-rtu-主站采集配置)
5. [模块详解](#5-模块详解)
   - [5.1 核心数据服务 (RemoteDataMgr)](#51-核心数据服务-remotedatamgr)
   - [5.2 Modbus TCP 主站 (modbus_tcp_master)](#52-modbus-tcp-主站-modbus_tcp_master)
   - [5.3 Modbus TCP 从站 (modbus_tcp_slave)](#53-modbus-tcp-从站-modbus_tcp_slave)
   - [5.4 PEMP2.0 遥测推送 (pemp_server)](#54-pemp20-遥测推送-pemp_server)
   - [5.5 调试控制台 (debug_console)](#55-调试控制台-debug_console)
   - [5.6 报文记录器 (packet_logger)](#56-报文记录器-packet_logger)
   - [5.7 双机冗余 (redundancy)](#57-双机冗余-redundancy)
   - [5.8 模块管理器与热重载](#58-模块管理器与热重载)
6. [PEMP2.0 通讯协议](#6-pemp20-通讯协议)
7. [编译与运行](#7-编译与运行)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

PMPC 四遥数据管理系统是一个跨平台的工业数据采集与转发平台，基于标准 C++17 开发，支持 Windows / Linux / macOS。系统以**四遥**（遥信 DI、遥测 AI、遥控 DO、遥调 AO）为核心数据模型，提供 Modbus TCP 主站采集、Modbus TCP 从站服务、PEMP2.0 协议推送、双机冗余、多线程并行处理、配置文件热重载等功能。

**核心特性：**

- **四遥数据表** —— 全局单例，通道→设备→点位分层，每设备独立锁并行读写
- **Modbus TCP 主站** —— 支持 FC01/02/03/04/05/06/15/16，模板化配置，连续地址自动合并
- **Modbus TCP 从站** —— 支持 FC01/02/03/04/05，点对点映射，地址可不连续，跨通道/设备
- **Modbus RTU 主站** —— 基于串口的 Modbus RTU 主站采集，支持物理串口 / TCP 串口服务器
- **Modbus RTU 从站** —— 串口 Modbus RTU 从站，FC01~05，点对点映射
- **IEC 60870-5-104 主站** —— 长连接 + 总召唤 + 主动上报，DI/AI/DO/AO 全支持
- **IEC 60870-5-104 从站** —— 对外提供 IEC 104 服务，GI 响应 + 遥控 + 主动上送
- **IEC 60870-5-103 主站** —— 继电保护设备采集，FUN + INF 映射
- **IEC 60870-5-101 主站** —— 串口远动协议，ASDU 层与 104 相同
- **IEC 60870-5-101 从站** —— 串口 IEC 101 从站，GI 响应 + 遥控
- **DLT 645 电表采集** —— 1997/2007 双版本，RS-485 串口，BCD 解析
- **CDT 主站** —— 循环式远动规约，被动接收 YC/YX/YM
- **CDT 从站** —— 循环式远动规约，主动上送
- **PEMP2.0 协议推送** —— TCP 端口 4096，DI/AI 独立上传间隔可配
- **双机冗余** —— 心跳检测 + 角色切换 + 数据同步 + DO 脉冲复位
- **动态模块管理** —— `app_modules.ini` 驱动，文件修改自动热重载
- **MySQL 数据存储** —— 实时值、DI 变化记录、AI 定时存盘，按月分表，多实例共享
- **调试控制台** —— TCP 端口 9090，交互式调试
- **报文记录器** —— 按设备分文件记录 Modbus 收发报文，协议自动解析
- **多字节序支持** —— 8 种字节序，int16/32/64、float、double，比例系数 + 偏移量
- **DI 点号约定** —— 点号 1 固定为设备通讯状态指示，业务遥信从点号 2 开始

---

## 2. 系统架构

### 2.1 数据层 (Data Layer)

全局唯一的四遥数据表。采用双层锁策略：

- `structMtx_` —— 保护通道/设备索引查找（微秒级持有）
- `Device::devMtx` —— 每设备独立互斥锁，同一通道不同设备的读写完全并行

### 2.2 采集层 (Collection Layer)

系统支持多种采集协议，均通过配置模板读取数据写入本地数据表。每通道/端口独立线程，断线自动重连，通讯状态变化时自动写入 DI 点号 1。

| 采集模块 | 传输层 | 数据标识 | 采集方式 |
|---------|--------|---------|---------|
| ModbusTcpMaster | TCP | addr+func | 轮询 |
| ModbusRtuMaster | 串口/TCP | addr+func | 轮询 |
| Iec104Master | TCP | IOA | 长连接 + 总召唤 |
| Iec103Master | 串口/TCP | FUN+INF | 查询 + 被动接收 |
| Iec101Master | 串口/TCP | IOA | 链路层轮询 |
| Dlt645Master | 串口/TCP | data_id | 轮询 |
| CdtMaster | 串口/TCP | addr | 被动接收 |

### 2.3 服务层 (Service Layer)

| 服务模块 | 协议 | 说明 |
|---------|------|------|
| **PempServer** | PEMP2.0 | 私有协议推送服务，端口 4096 |
| **ModbusTcpSlave** | Modbus TCP | 对外提供 Modbus TCP 从站 |
| **ModbusRtuSlave** | Modbus RTU | 对外提供 Modbus RTU 从站（串口） |
| **Iec104Slave** | IEC 104 | 对外提供 IEC 104 从站服务 |
| **Iec101Slave** | IEC 101 | 对外提供 IEC 101 从站（串口） |
| **CdtSlave** | CDT | 主动循环上送 CDT 帧 |

### 2.4 辅助层 (Utility Layer)

- **DebugConsole** —— TCP 调试控制台
- **PacketLogger** —— 报文记录器
- **EventBus** —— 类型安全事件发布/订阅（RemoteDataMgr 的 Set 接口自动发布 DIChange/AIChange 等事件）
- **DataRecorder** —— MySQL 数据存储模块，订阅 EventBus 写入数据库
- **CommIO** —— 统一 IO 封装，`port_name` 包含 `:` 自动走 TCP，否则走物理串口，所有串口模块共用

### 2.5 模块管理

ModuleManager 解析 `app_modules.ini`，创建 AppModule 子类实例，管理启动/停止/热重载。后台线程每 2 秒轮询配置文件修改时间，检测到变化自动执行 `Stop → LoadConfig → Start`。

---

## 3. 文件结构

```
d:\test\pmpc\
│
├── app_modules.ini              入口模块配置
├── modbus_tcp_master.ini        Modbus TCP 主站采集配置
├── modbus_tcp_slave.ini         Modbus TCP 从站配置
├── modbus_rtu_master.ini        Modbus RTU 主站采集配置
├── modbus_rtu_slave.ini         Modbus RTU 从站配置
├── iec104_master.ini            IEC 104 主站采集配置
├── iec104_slave.ini             IEC 104 从站配置
├── iec103_master.ini            IEC 103 主站采集配置
├── iec101_master.ini            IEC 101 主站采集配置
├── iec101_slave.ini             IEC 101 从站配置
├── dlt645_master.ini            DLT 645 电表采集配置
├── cdt_master.ini               CDT 主站配置
├── cdt_slave.ini                CDT 从站配置
├── pemp_server.ini              PEMP2.0 推送服务配置
├── point_cfg.ini                四遥点表配置
├── debug_console.ini            调试控制台配置
├── packet_logger.ini            报文记录器配置
├── data_recorder.ini            MySQL 数据存储配置
├── redundancy.ini           双机冗余配置
├── CMakeLists.txt               CMake 构建文件
├── CLAUDE.md                    项目说明 (Claude Code)
│
├── include/
│   ├── pmpc.h                   四遥数据管理器
│   ├── modbus_tcp_master.h      Modbus TCP 主站框架
│   ├── modbus_tcp_slave.h       Modbus TCP 从站框架
│   ├── modbus_rtu_master.h      Modbus RTU 主站框架
│   ├── modbus_rtu_slave.h       Modbus RTU 从站框架
│   ├── iec104_master.h          IEC 104 主站框架
│   ├── iec104_slave.h           IEC 104 从站框架
│   ├── iec103_master.h          IEC 103 主站框架
│   ├── iec101_master.h          IEC 101 主站框架
│   ├── iec101_slave.h           IEC 101 从站框架
│   ├── dlt645_master.h          DLT 645 电表采集
│   ├── cdt_master.h             CDT 主站
│   ├── cdt_slave.h              CDT 从站
│   ├── comm_io.h                串口/TCP 统一 IO
│   ├── module_manager.h         模块管理器
│   ├── module_factory.h         模块工厂（自动注册）
│   ├── protocol.h               PEMP2.0 协议引擎
│   ├── pemp_server.h            PEMP2.0 推送服务
│   ├── redundancy.h             双机冗余管理器
│   ├── debug_console.h          调试控制台
│   ├── packet_logger.h          报文记录器 + 协议解析器
│   ├── data_recorder.h          MySQL 数据存储
│   ├── event_bus.h              事件总线
│   ├── soe_queue.h              SOE 事件队列
│   ├── socket.h                 Socket 封装（RAII）
│   ├── ini_reader.h             INI 文件解析器
│   ├── str_util.h               字符串工具
│   └── serial_port.h            串口封装
│
├── src/
│   ├── main.cxx                 主入口
│   ├── pmpc_data_mgr.cxx        数据管理器实现
│   ├── pmpc_config_reader.cxx   INI 解析 + 点表加载
│   ├── modbus_tcp_master.cxx    Modbus TCP 主站
│   ├── modbus_tcp_slave.cxx     Modbus TCP 从站
│   ├── modbus_rtu_master.cxx    Modbus RTU 主站
│   ├── modbus_rtu_slave.cxx     Modbus RTU 从站
│   ├── iec104_master.cxx        IEC 104 主站
│   ├── iec104_slave.cxx         IEC 104 从站
│   ├── iec103_master.cxx        IEC 103 主站
│   ├── iec101_master.cxx        IEC 101 主站
│   ├── iec101_slave.cxx         IEC 101 从站
│   ├── dlt645_master.cxx        DLT 645 电表
│   ├── cdt_master.cxx           CDT 主站
│   ├── cdt_slave.cxx            CDT 从站
│   ├── pemp_server.cxx          PEMP2.0 推送服务
│   ├── module_manager.cxx       模块管理 + 热重载
│   ├── redundancy.cxx           双机冗余
│   ├── debug_console.cxx        调试控制台
│   ├── packet_logger.cxx        报文记录器
│   ├── data_recorder.cxx        MySQL 数据存储
│   ├── protocol.cxx             协议引擎
│   ├── soe_queue.cxx            SOE 事件队列
│   ├── ini_reader.cxx           INI 解析器
│   ├── socket.cpp               Socket 实现
│   └── serial_port.cpp          串口实现
│
└── logs/                        错误日志 / 报文记录目录
    ├── config_error_*.log
    └── traffic/
        ├── ch1_dev1_20260626.log
        └── ch1_dev2_20260626.log
```

---

## 4. 配置说明

系统使用 INI 格式配置文件。编码统一为 UTF-8（无 BOM），注释使用 `;` 或 `#` 开头。

---

### 4.1 point_cfg.ini —— 四遥点表配置

定义四遥数据的通道/设备/点位分层结构。系统启动时加载构建全局数据表。

**DI 点号约定：**
- **点号 1** 固定为设备通讯状态指示（ON=在线, OFF=离线），由 `modbus_tcp_master` 自动维护
- 业务遥信从 **点号 2** 开始
- 配置文件中 `diCnt` 表示业务遥信数量，系统自动多分配 1 个点给通讯状态（总计 `diCnt + 1` 个 DI）

```ini
[global]
desc=四遥点位数量配置

[Channel_1]
Dev_1=35,42,8,2          ; DI=35(业务) AI=42 DO=8 AO=2
                         ; 实际 DI 总数 = 36 (点号 1~36，点号 1 为通讯状态)
Dev_2=28,36,1            ; DI=28 AI=36 DO=1

[Channel_2]
Dev_1=20,30,4,0          ; 无 AO

[Channel_3]
Dev_1=60,80,12,5
Dev_2=32,45,5,1
Dev_3=18,26,3,0
```

---

### 4.2 app_modules.ini —— 模块主配置

入口配置，定义启动哪些模块、引用哪个配置文件、是否热重载。

```ini
[global]
watch_interval=2             ; 文件监控轮询间隔（秒）

[modbus_tcp_master]
; Modbus TCP 主站采集
enable=1
cfg_file=modbus_tcp_master.ini
auto_reload=1

[modbus_tcp_slave]
; Modbus TCP 从站（FC01/02/03/04/05）
enable=1
cfg_file=modbus_tcp_slave.ini
auto_reload=1

[modbus_rtu_master]
; Modbus RTU 主站采集（串口）
enable=0
cfg_file=modbus_rtu_master.ini
auto_reload=0

[modbus_rtu_slave]
; Modbus RTU 从站（串口）
enable=0
cfg_file=modbus_rtu_slave.ini
auto_reload=0

[iec104_master]
; IEC 60870-5-104 主站
enable=0
cfg_file=iec104_master.ini
auto_reload=0

[iec104_slave]
; IEC 60870-5-104 从站
enable=0
cfg_file=iec104_slave.ini
auto_reload=0

[iec103_master]
; IEC 60870-5-103 主站
enable=0
cfg_file=iec103_master.ini
auto_reload=0

[iec101_master]
; IEC 60870-5-101 主站（串口）
enable=0
cfg_file=iec101_master.ini
auto_reload=0

[iec101_slave]
; IEC 60870-5-101 从站（串口）
enable=0
cfg_file=iec101_slave.ini
auto_reload=0

[dlt645_master]
; DLT 645 电表采集（串口）
enable=0
cfg_file=dlt645_master.ini
auto_reload=0

[cdt_master]
; CDT 主站（被动接收）
enable=0
cfg_file=cdt_master.ini
auto_reload=0

[cdt_slave]
; CDT 从站（主动上送）
enable=0
cfg_file=cdt_slave.ini
auto_reload=0

[packet_logger]
; 报文记录器
enable=1
cfg_file=packet_logger.ini
auto_reload=0              ; 运行时一般不修改，无需热重载

[debug_console]
; TCP 调试控制台（端口 9090），生产环境建议关闭
enable=1
cfg_file=debug_console.ini
auto_reload=0

[data_recorder]
; MySQL 数据存储模块
enable=1
cfg_file=data_recorder.ini
auto_reload=0

[pemp_server]
; PEMP2.0 TCP 遥测推送服务
enable=1
cfg_file=pemp_server.ini
auto_reload=1

[redundancy]
; 双机冗余（心跳+数据同步+角色切换）
enable=0                   ; 单机运行时关闭
cfg_file=redundancy.ini
auto_reload=1
```

**配置项说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `enable` | 0/1 | 是否启用该模块 |
| `cfg_file` | 路径 | 模块对应的配置文件名 |
| `auto_reload` | 0/1 | 配置文件修改后是否自动热重载 |

---

### 4.3 modbus_tcp_master.ini —— Modbus TCP 主站采集配置

采用 `global → Channel → Template → Device` 四级继承链：

- **global** —— 默认参数（timeout/retry/verbose），被各通道继承
- **Channel** —— TCP 连接配置（ip/port/scan_ms），每通道独立线程
- **Template** —— 型号级解析模板，可被多个设备引用
- **Device** —— 实际设备实例，引用模板 + 追加自定义条目

```ini
[global]
timeout_ms=3000
retry_count=3
retry_sleep_ms=10000
comm_loss=保持
verbose=2
hex_dump=1

[Channel_1]
ip=192.168.1.100                 ; 主网口 IP
port=502                         ; 主网口端口
standby_ip=192.168.1.101         ; 备网口 IP（单机主备网口冗余）
standby_port=502                 ; 备网口端口（默认同主端口）
fallback=1                       ; 备线断后自动回切主线（1=开启）
scan_ms=3000

[Template_保护装置]
; ── DI 遥信采集 ──
; FC01 读线圈 → DI（业务遥信从 point=2 开始）
di_001 = addr=0, func=01, qty=32, point=2, invert=0, comm_loss=保持
; FC03 按位读寄存器 → DI
di_002 = addr=100, func=03, bit=0, point=34, invert=0, comm_loss=保持
; ── AI 遥测采集 ──
; FC03 读寄存器 → AI
ai_001 = addr=0, func=03, point=1, dtype=uint16, endian=AB, scale=1.0, offset=0
ai_002 = addr=4, func=03, point=3, dtype=float, endian=CDAB, scale=0.01, offset=0
; ── DO 遥控写回 ──
; FC05 写单线圈（主站下发 masterVal 变化时自动写回 Modbus）
do_001 = addr=0, func=05, point=1, invert=0
do_002 = addr=1, func=05, point=2, invert=0
; ── AO 遥调写回 ──
; FC06 写单寄存器（主站下发 AO 变化时自动写回 Modbus）
ao_001 = addr=10, func=06, point=1, dtype=uint16
ao_002 = addr=12, func=06, point=2, dtype=uint16, scale=0.01

[Device_1]
channel=1
station_id=1
template=保护装置
desc=1号保护设备

[Device_2]
channel=1
station_id=2
template=保护装置
desc=2号保护设备
```

**Channel 配置字段：**

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `ip` | 主网口 IP 地址 | 必填 |
| `port` | 主网口 TCP 端口 | 502 |
| `standby_ip` | 备网口 IP（留空=不启用主备切换） | 空 |
| `standby_port` | 备网口 TCP 端口 | 同 `port` |
| `fallback` | 备线断后自动回切主网口（1=开启） | 1 |
| `scan_ms` | 采集间隔（毫秒） | 3000 |
| `timeout_ms` | 超时时间（毫秒） | 继承 global |
| `retry_count` | 重试次数 | 继承 global |
| `retry_sleep_ms` | 重试等待（毫秒） | 继承 global |
| `verbose` | 日志级别 | 继承 global |
| `hex_dump` | HEX 报文输出 | 继承 global |
| `max_di_read` | FC01/02 单包最大线圈数 | 2000 |
| `max_ai_read` | FC03/04 单包最大寄存器数 | 125 |

**DI 映射字段：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `addr` | Modbus 地址 | 0~65535 |
| `func` | 功能码 | 1(读线圈)/2(读离散输入)/3(读保持寄存器)/4(读输入寄存器) |
| `qty` | 数量（FC01/02） | 1~2000 |
| `bit` | 位号（FC03/04 按位读 DI） | 0~15，-1=不适用 |
| `point` | 本地 DI 起始点号 | 从 2 开始（点号 1 为通讯状态） |
| `invert` | 是否取反 | 0/1 |
| `comm_loss` | 通讯中断处理 | 保持/归零/最大值/最小值/(需自定义值) |

**AI 映射字段：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `addr` | Modbus 起始地址 | 0~65535 |
| `func` | 功能码 | 3(保持寄存器)/4(输入寄存器) |
| `point` | 本地 AI 点号 | 1~65535 |
| `dtype` | 数据类型 | uint16/int16/uint32/int32/uint64/int64/float/double |
| `endian` | 字节序 | AB/BA/CDAB/ABCD/BADC/DCBA/ABCDEFGH/HGFEDCBA |
| `scale` | 系数 | 默认 1.0 |
| `offset` | 偏移量 | 默认 0.0 |
| `comm_loss` | 通讯中断处理 | 保持/归零/最大值/最小值/自定义 |

**工程值 = 原始值 × scale + offset**

**DO 映射字段（写回）：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `addr` | Modbus 线圈地址 | 0~65535 |
| `func` | 功能码 | 5(写单线圈)/15(写多线圈)，FC15 暂简化为 FC05 |
| `point` | 本地 DO 点号 | 1~65535 |
| `invert` | 写回前是否取反 masterVal | 0/1 |
| `pulse_ms` | 脉冲宽度（毫秒），0=不发送复位报文 | ≥0，默认 0 |

**AO 映射字段（写回）：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `addr` | Modbus 寄存器起始地址 | 0~65535 |
| `func` | 功能码 | 6(写单寄存器)/16(写多寄存器)，FC16 待扩展 |
| `point` | 本地 AO 点号 | 1~65535 |
| `dtype` | 数据类型 | uint16(当前仅支持) |
| `endian` | 字节序 | AB |
| `scale` | 系数 | 默认 1.0 |
| `offset` | 偏移量 | 默认 0.0 |
| `pulse_ms` | 脉冲宽度（毫秒），0=不发送复位报文 | ≥0，默认 0 |

**DO/AO 写回机制：**

DO/AO 写回在每次采集循环（`PollDevice`）之后执行。流程如下：

```
采集循环开始 → PollDevice(读 DI/AI) → WriteDOChanges → WriteAOChanges → 更新通讯状态 → sleep(scanMs)
```

- **WriteDOChanges**：遍历设备的所有 DO 映射条目，读取本地 `DoPoint.masterVal`，与缓存值 `lastMaster` 比较。仅当值变化时构建 FC05 请求写回 Modbus 设备。支持 `invert` 取反后写入。
- **WriteAOChanges**：遍历设备的所有 AO 映射条目，读取本地 `AoPoint.value`，与缓存值 `lastVal` 比较（差值 > 0.001 视为变化）。变化时构建 FC06 请求写回。
- FC05/FC06 均采用一发一答模式：发送后等待响应，超时或异常码跳过继续。

**配置示例：在 Template 中定义 DO/AO 映射**

```ini
[Template_保护装置]
; ── DI 采集 ──
di_001 = addr=0, func=01, qty=32, point=2, invert=0
di_002 = addr=100, func=03, bit=0, point=34
; ── AI 采集 ──
ai_001 = addr=0, func=03, point=1, dtype=uint16, endian=AB, scale=1.0
ai_002 = addr=4, func=03, point=3, dtype=float, endian=CDAB, scale=0.01
; ── DO 遥控写回（FC05 写单线圈） ──
do_001 = addr=0, func=05, point=1, invert=0
do_002 = addr=1, func=05, point=2, invert=0
; ── AO 遥调写回（FC06 写单寄存器） ──
ao_001 = addr=10, func=06, point=1, dtype=uint16
ao_002 = addr=12, func=06, point=2, dtype=uint16, scale=0.1, offset=0
```

**在 Device 中追加自定义 DO/AO**

```ini
[Device_1]
channel=1
station_id=1
template=保护装置
desc=1号保护设备
; 此设备额外追加一个遥控点（写入 Modbus 地址 50）
do_101 = addr=50, func=05, point=5, invert=0
; 此设备额外追加一个遥调点（写入 Modbus 地址 60）
ao_101 = addr=60, func=06, point=5, dtype=uint16
```

**DO invert 示例：**

`invert=1` 时，主站下发 `masterVal=1` 实际向 Modbus 写入 0（OFF），`masterVal=0` 写入 0xFF00（ON）。适用于常闭触点场景：

```ini
; 本地 DO 点号 3 → Modbus 地址 5，写入前取反
do_003 = addr=5, func=05, point=3, invert=1
```

**DO/AO 脉冲复位（pulse_ms）：**

`pulse_ms` 控制写回后是否自动发送复位报文，实现可控宽度的脉冲输出：

- **DO 脉冲**：写入 `masterVal` 对应值后，等待 `pulse_ms` 毫秒，自动写入相反值（ON→OFF 或 OFF→ON）
- **AO 脉冲**：写入设定值后，等待 `pulse_ms` 毫秒，自动写入 `0`

适用于断路器合闸脉冲（合闸后自动分闸）、报警复位等需要短暂 ON 脉冲的场景。

```ini
; DO 脉冲示例：合闸后 300ms 自动分闸
do_001 = addr=0, func=05, point=1, invert=0, pulse_ms=300

; AO 脉冲示例：输出设定值后 1 秒自动归零
ao_001 = addr=10, func=06, point=1, dtype=uint16, pulse_ms=1000
```

**主备网口 / 双机冗余说明：**

modbus_tcp_master 支持两层冗余：

**① 单机主备网口（`standby_ip`/`standby_port`/`fallback`）**

单台机器配置主备两个网口连接同一批现场设备。主网口断线后自动切到备网口，备线断后自动回切主线：

```ini
[Channel_1]
ip=192.168.1.100              ; 主线 IP（接交换机 A）
standby_ip=192.168.1.101      ; 备线 IP（接交换机 B）
port=502
fallback=1                    ; 备线断后切回主线
```

采集线程连接顺序：先主线，失败则试备线，全断则按 `retry_count` 重试。`fallback=1` 时，备线断开后下次重连回主线。

**② 双机冗余（redundancy 模块联动）**

modbus_tcp_master 是**采集模块**，受冗余模块联动控制：
- Master 角色：自动启动采集，连接现场 PLC/RTU
- Standby 角色：自动停止采集，不发起 TCP 连接
- 主备机 `Channel_N` 的 `ip`/`port` 通常**完全相同**（连接同一批现场设备）
- 若主备机分属不同网段，可各自配置不同的 `ip` 地址
- 单机主备网口与双机冗余可叠加使用：主机自身有 `standby_ip`，备机也有自己的主备网口

---

### 4.4 modbus_tcp_slave.ini —— Modbus TCP 从站配置

对外提供 Modbus TCP 从站服务。**点对点映射**：每个条目对应一个 Modbus 地址到一个本地点，地址**可以不连续**，可**跨通道/设备**。

各功能码独立地址空间：

| 功能码 | 说明 | 映射到 |
|--------|------|--------|
| 01 | 读线圈 | 本地遥信 DI |
| 02 | 读离散输入 | 本地遥信 DI |
| 03 | 读保持寄存器 | 本地遥测 AI（含 dtype/scale/offset）或按位读遥信 DI |
| 04 | 读输入寄存器 | 同上 |
| 05 | 写单个线圈 | 本地遥控 DO（支持 invert 反相） |

```ini
[global]
port=503                  ; 监听端口（避免与 Master 的 502 冲突）
max_clients=10            ; 最大并发客户端数
verbose=1

[Device_1]
station_id=1
desc=1号从站

; ── FC01: 读线圈 → 本地 DI（业务遥信从 point=2 开始）──
fc01_001 = addr=0,  ch=1, dev=1, point=2
fc01_002 = addr=1,  ch=1, dev=1, point=3     ; 不连续地址
fc01_003 = addr=5,  ch=1, dev=1, point=4
fc01_004 = addr=10, ch=1, dev=2, point=2     ; 跨设备
fc01_005 = addr=11, ch=2, dev=1, point=2     ; 跨通道

; ── FC02: 读离散输入 → 本地 DI ──
fc02_001 = addr=0, ch=1, dev=1, point=2
fc02_002 = addr=8, ch=2, dev=1, point=2

; ── FC03: 读保持寄存器 → 本地 AI ──
fc03_001 = addr=0,  ch=1, dev=1, point=1, dtype=uint16
fc03_002 = addr=2,  ch=1, dev=1, point=2, dtype=float, scale=0.01
fc03_003 = addr=10, ch=2, dev=1, point=1, dtype=uint16, scale=0.1, offset=-50

; ── FC03 按位读遥信 ──
;   同时存在 AI 和 DI 时，AI 先行写入，DI 按位叠加
fc03_101 = addr=100, bit=0, ch=1, dev=1, point=2
fc03_102 = addr=100, bit=1, ch=1, dev=1, point=3   ; 同一寄存器不同位
fc03_103 = addr=101, bit=0, ch=2, dev=1, point=2   ; 跨设备

; ── FC04: 读输入寄存器 → 本地 AI ──
fc04_001 = addr=0, ch=1, dev=1, point=1, dtype=uint16
fc04_002 = addr=3, ch=2, dev=1, point=1, dtype=float, scale=0.01

; ── FC05: 写单个线圈 → 本地 DO ──
fc05_001 = addr=0, ch=1, dev=1, point=1
fc05_002 = addr=1, ch=1, dev=1, point=2, invert=1

[Device_2]
station_id=2
desc=2号从站

fc01_001 = addr=0, ch=1, dev=1, point=2
fc03_001 = addr=0, ch=1, dev=1, point=1, dtype=uint16
fc05_001 = addr=0, ch=1, dev=1, point=1
```

---

### 4.5 pemp_server.ini —— PEMP2.0 推送服务配置

```ini
[global]
port=4096                ; PEMP2.0 服务端口
di_upload_ms=5000        ; 遥信主动上传间隔（毫秒），0=不上传
ai_upload_ms=5000        ; 遥测主动上传间隔（毫秒），0=不上传
rc_password=             ; 遥控密码（空=不校验）
clock_sync_enable=0      ; 收到 08H 同步时钟帧时是否输出日志
clock_sync_verbose=0     ; 设为 1 每次同步都记录，0 仅记录首次
```

**新增功能：**
- **遥控密码**（`rc_password`）：设置后客户端 06H 帧必须携带匹配的密码字节才能执行遥控
- **时钟同步日志**（`clock_sync_enable`/`clock_sync_verbose`）：08H 帧接收日志，便于运维核对主站时钟同步

**主动上传说明：**

- **DI 遥信** —— 通过 51H 帧，按 `di_upload_ms` 间隔上传
- **AI 遥测** —— 通过 53H 帧，按 `ai_upload_ms` 间隔上传
- **SOE** —— 通过 52H 帧，在 DI/AI 上传间隙自动上传
- DI/AI 各自独立计时，互不影响
- 设为 0 可单独关闭某类上传
- 一发一答约束：发送主动数据后需等待 ACK 或超时（1500ms）才能发送下一条

---

### 4.6 debug_console.ini —— 调试控制台配置

```ini
[global]
port=9090                  ; TCP 调试端口
verbose=1                  ; 日志级别
; password=admin123        ; 密码认证（注释掉则不认证）
```

**安全特性：**
- 密码采用 constant-time 比较，防止 timing 侧信道攻击
- 每连接最多 3 次尝试，每次失败后自动等待 1 秒，降低暴力破解效率
- 认证失败后立即断开连接，不提供任何提示信息

**监听绑定：** 可限制只允许特定 IP 接入：

```ini
[listen_1]
port=9090
ip=127.0.0.1              ; 仅允许本机访问调试控制台

[listen_2]
port=9091
ip=192.168.1.0/24         ; 允许局域网段访问（仅当 enable_ip_mask=1 时有效）
```

连接方式：

```bash
telnet 127.0.0.1 9090
```

连接后自动打印使用帮助。如果配置了 `password`，连接后需先输入密码（3 次机会）。

支持以下命令：

| 命令 | 功能 | 示例 |
|------|------|------|
| `get di/ai/do/ao <ch> <dev> [pt]` | 读取四遥值 | `get di 1 1` / `get ai 1 1 5` |
| `set di/ai/do/ao <ch> <dev> <pt> <val>` | 设置四遥值 | `set di 1 1 3 1` / `set ai 1 1 2 23.45` |
| `auto di/ai <ch> <dev> <pt> <ms>` | 自动变位 | `auto di 1 1 10 500`（DI 每 500ms 翻转） |
| `auto stop <id\|all>` | 停止自动变位 | `auto stop 1` / `auto stop all` |
| `role [master\|standby\|idle]` | 查看/切换冗余角色 | `role master` |
| `status` | 模块运行状态 | `status` |
| `channels` | 通道列表 | `channels` |
| `devices <ch>` | 设备列表 | `devices 1` |
| `start\|stop <模块名>` | 启停模块 | `start modbus_tcp_master` |
| `reload [point\|模块名]` | 重载配置 | `reload point` / `reload modbus_tcp_slave` |
| `log start\|stop\|status\|parse <on\|off>` | 报文记录控制 | `log start 1 1` / `log parse off` |
| `help` / `h` / `?` | 帮助 | `help` |
| `quit` / `exit` | 断开 | `quit` |

---

### 4.7 packet_logger.ini —— 报文记录器配置

```ini
[global]
log_dir=logs/traffic    ; 日志文件输出目录
parse=1                 ; 是否解析报文内容（1=解析, 0=仅 HEX）
max_days=30             ; 日志文件保留天数
```

日志文件格式：

```
logs/traffic/ch{N}_dev{N}_YYYYMMDD.log
```

日志条目示例：

```
[2026-06-26 19:00:00.123] [TX] ch=1 dev=1 station=1 FC03 trans=1 len=5
  hex: 03 00 00 00 0a
  --> [ModbusTCP] FC03 读保持寄存器 addr=0 qty=10

[2026-06-26 19:00:00.234] [RX] ch=1 dev=1 station=1 FC03 trans=1 len=23
  hex: 03 14 00 01 00 02 ...
  --> [ModbusTCP] 响应 10 个寄存器
```

**协议解析器扩展：**

报文记录器通过 `ProtocolParser` 插件机制自动解析报文。内置 `ModbusTcpParser` 支持 FC01~06/0F/10。

新增协议只需：

1. 继承 `ProtocolParser` 基类
2. 实现 `CanParse(ParseContext)` 和 `Parse(ParseContext)`
3. 通过 `logger.RegisterParser(std::make_unique<MyParser>())` 注册

无需修改 PacketLogger 本身，也无需修改调用方（Master/Slave）。

---

### 4.8 redundancy.ini —— 双机冗余配置

```ini
[global]
; 本机标识
local_name=box_a

; 对方 IP 地址
peer_ip=127.0.0.1

; 心跳 TCP 端口
heartbeat_port=7503

; 数据同步 TCP 端口
sync_port=7502

; 心跳间隔（毫秒）
heartbeat_interval_ms=1000

; 连续丢失多少个心跳判定对方离线
missed_heartbeat_limit=5

; 主机恢复后是否自动切回
auto_failback=1

; 启动后延迟决策角色的时间（毫秒）
startup_delay_ms=3000

; 主机优先级（值大的优先成为主机）
priority=100
```

**实际双机组网示例：**

两台 PMPC 实例通过局域网组双机冗余。主机 A 和备机 B 的配置文件差异仅在于 `local_name`、`peer_ip` 和 `priority`，其他参数一致。

**机器 A（主站优先，IP=192.168.1.10）：**

```ini
[global]
local_name=station_a               ; 本机标识（唯一）
peer_ip=192.168.1.11               ; 对方 IP（机器B）
heartbeat_port=7503
sync_port=7502
heartbeat_interval_ms=1000
missed_heartbeat_limit=5
auto_failback=1
startup_delay_ms=3000
priority=200                       ; 优先级高，启动后优先成为主机
```

**机器 B（备机，IP=192.168.1.11）：**

```ini
[global]
local_name=station_b               ; 本机标识（唯一）
peer_ip=192.168.1.10               ; 对方 IP（机器A）
heartbeat_port=7503
sync_port=7502
heartbeat_interval_ms=1000
missed_heartbeat_limit=5
auto_failback=1
startup_delay_ms=3000
priority=100                       ; 优先级低，启动后成为备机
```

> **注意：** 两台机器的端口号（`heartbeat_port`、`sync_port`）必须相同。防火墙需放行这两个端口的 TCP 流量。主机 A 恢复后 `auto_failback=1` 会自动切回，避免备机长期接管。

---

### 4.9 data_recorder.ini —— MySQL 数据存储配置

将四遥数据存入 MySQL，支持多台 PMPC 实例共享同一数据库。可配置需要记录的点和标签名，AI 可配置独立存盘间隔。

```ini
[global]
; 数据源标识（必填，多台PMPC靠此字段区分）
source=192.168.1.100
; MySQL 连接
host=127.0.0.1
port=3306
db=pmpc_data
user=root
password=root
; 记录控制
enabled=1
ai_interval_ms=5000

[di_points]
DI_保护1_状态 = ch=1, dev=1, pt=2
DI_保护1_告警 = ch=1, dev=1, pt=3

[ai_points]
AI_母线电压 = ch=1, dev=1, pt=1, interval_ms=5000
AI_有功功率 = ch=1, dev=1, pt=2, interval_ms=300000

[do_points]
DO_断路器1 = ch=1, dev=1, pt=1
```

**数据库表：**

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `rt_status` | 实时值 | `source,tag,ch,dev,pt,type,value,ts_ms` |
| `di_log_YYYYMM` | DI 变化记录（按月） | `source,tag,old_val,new_val,ts_ms` |
| `ai_log_YYYYMM` | AI 定时存盘（按月） | `source,tag,value,ts_ms`(时间对齐) |
| `pmpc_instance` | 实例在线 | `source,last_seen` |
| `schema_version` | 版本管理 | `version,ver_desc` |

多台 PMPC 实例共享同一数据库，通过 `source` 字段区分数据来源。AI 时间对齐公式：`aligned_ms = floor(ts / interval) * interval`。按月分表每月自动创建。

---

### 4.10 modbus_rtu_master.ini —— Modbus RTU 主站采集配置

采用 `global → Channel → Template → Device` 四级继承链，与 TCP 主站结构一致，区别在于串口参数配置。支持通过 `CommIO` 使用物理串口或 TCP 串口服务器。

```ini
[global]
timeout_ms=1000
retry_count=2
retry_sleep_ms=5000
verbose=1

[Channel_1]
port_name=COM1              ; 物理串口，或 192.168.1.100:10000 (TCP)
baud=9600
parity=even
data_bits=8
stop_bits=1
scan_ms=3000

[Template_保护装置]
; FC01 读线圈 → DI
di_001 = addr=0, func=01, qty=32, point=2
; FC03 读寄存器 → AI
ai_001 = addr=0, func=03, point=1, dtype=uint16, scale=1.0, offset=0
ai_002 = addr=4, func=03, point=3, dtype=float, scale=0.01, offset=0

[Device_1]
channel=1
station_id=1
template=保护装置
desc=1号保护设备
```

**端口参数说明：**

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `port_name` | 串口号 | COM1 |
| `baud` | 波特率 | 9600 |
| `parity` | 校验位（none/even/odd） | even |
| `data_bits` | 数据位 | 8 |
| `stop_bits` | 停止位（1/2） | 1 |
| `scan_ms` | 采集间隔（毫秒） | 3000 |
| `timeout_ms` | 超时时间（毫秒） | 1000 |

**支持功能码：** FC01(读线圈) FC02(读离散输入) FC03(读保持寄存器) FC04(读输入寄存器)。可使用 DI/AI 映射模板，格式同 `modbus_tcp_master.ini`。DI 业务遥信从 point=2 开始。

**双机冗余说明：**

modbus_rtu_master 是**采集模块**，受冗余模块联动控制。主备机的串口配置可能不同：

```ini
; 主机 A（物理串口 COM1）
[Channel_1]
port_name=COM1
baud=9600
parity=even
scan_ms=3000
```

```ini
; 备机 B（同一 RS-485 总线，物理串口 COM3）
[Channel_1]
port_name=COM3
baud=9600
parity=even
scan_ms=3000
```

主备机其他参数（baud/parity/模板/映射）应保持一致。若使用 TCP 串口服务器，则主备机 `port_name` 均为 `IP:PORT`，无需区分。

---

### 4.11 modbus_rtu_slave.ini —— Modbus RTU 从站配置

串口 Modbus RTU 从站，配置格式同 `modbus_tcp_slave.ini`。支持物理串口和 TCP 串口服务器。

```ini
[global]
port_name=COM1          ; 或 192.168.1.100:10000
baud=9600  parity=even  data_bits=8  stop_bits=1
verbose=1

[Device_1]  station_id=1  desc=1号从站
fc01_001 = addr=0, ch=1, dev=1, point=2
fc03_001 = addr=0, ch=1, dev=1, point=1, dtype=uint16
fc05_001 = addr=0, ch=1, dev=1, point=1
```

---

### 4.12 iec104_master.ini —— IEC 104 主站配置

`global → Channel → Template → Device` 层次。IOA 寻址，支持 IEC 104 长连接 + 总召唤 + 时钟同步。

```ini
[global]
timeout_ms=5000
retry_count=3
retry_sleep_ms=10000
gi_cycle_s=300
clock_sync_interval_s=3600
verbose=1

[Channel_1]
ip=192.168.1.10                ; 主网口 IP
port=2404                      ; 主网口端口
standby_ip=192.168.1.11        ; 备网口 IP（可选）
standby_port=2404              ; 备网口端口（默认同主端口）
fallback=1                     ; 备线断后自动回切主线
timeout_ms=5000
retry_count=3
retry_sleep_ms=10000
gi_cycle_s=300                 ; 总召唤周期（秒）
clock_sync_interval_s=3600     ; 时钟同步周期（秒）
t0=30  t1=15  t2=10  t3=20    ; IEC 104 连接参数（默认即可）
k=12   w=8

[Template_保护装置]
di_001 = ioa=1000, point=2
ai_001 = ioa=2000, type=M_ME_NC_1, point=1, scale=0.01
do_001 = ioa=3000, val=0, point=1    ; 分闸
do_002 = ioa=3000, val=1, point=2    ; 合闸

[Device_1]
channel=1
common_addr=1
template=保护装置
desc=1号RTU
```

**Channel 配置字段：**

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `ip` | 主网口 IP 地址 | 必填 |
| `port` | 主网口 TCP 端口 | 2404 |
| `standby_ip` | 备网口 IP（留空=不启用主备切换） | 空 |
| `standby_port` | 备网口 TCP 端口 | 同 `port` |
| `fallback` | 备线断后自动回切主网口（1=开启） | 1 |
| `timeout_ms` | 超时时间（毫秒） | 5000 |
| `retry_count` | 重试次数 | 3 |
| `retry_sleep_ms` | 重试等待（毫秒） | 10000 |
| `gi_cycle_s` | 总召唤间隔（秒） | 300 |
| `clock_sync_enable` | 时钟同步开关 | 1（开启） |
| `clock_sync_interval_s` | 时钟同步间隔（秒） | 3600 |
| `spontaneous_enable` | 主动上报开关 | 1（开启） |
| `t0` | 连接超时（秒） | 30 |
| `t1` | 发送/测试超时（秒） | 15 |
| `t2` | 接收确认延迟（秒） | 10 |
| `t3` | 测试帧发送间隔（秒） | 20 |
| `k` | 最大未确认 I 帧数 | 12 |
| `w` | 最大未确认接收帧数 | 8 |
| `verbose` | 日志级别 | 继承 global |

**DI 映射字段：**

| 字段 | 说明 |
|------|------|
| `ioa` | IEC 104 IOA 地址 |
| `point` | 本地 DI 起始点号（从 2 开始） |

**AI 映射字段：**

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `ioa` | IEC 104 IOA 地址 | 0~16777215 |
| `point` | 本地 AI 点号 | 1~65535 |
| `type` | ASDU 类型 | M_ME_NA_1(归一化值) / M_ME_NC_1(浮点) / M_ME_NB_1(标度化值) |
| `scale` | 工程转换系数 | 默认 1.0 |
| `offset` | 工程转换偏移 | 默认 0.0 |

**双连接冗余与双机冗余说明：**

IEC 104 主站支持两层冗余：

**① 单机主备网口（`standby_ip`/`standby_port`/`fallback`）**

单台机器配置主备两个网口连接同一批 RTU。主网口断线后自动切到备网口，备线断后自动回切主线。连接行为与 `modbus_tcp_master` 相同，详见 [4.3 主备网口说明](#43-modbus_tcp_masterini--modbus-tcp-主站采集配置)。

**② 双机冗余（redundancy 模块联动）**

iec104_master 是**采集模块**，受冗余模块联动控制：
- Master 角色：自动启动采集，连接现场 RTU
- Standby 角色：自动停止采集，不发起 TCP 连接
- 主备机 `Channel` 的 `ip`/`port` 通常相同（连接同一批 RTU）
- 单机主备网口与双机冗余可叠加使用

---

### 4.13 iec104_slave.ini —— IEC 104 从站配置

对外提供 IEC 104 服务，响应主站 GI（总召唤）、遥控、时钟同步，支持 DI/AI 主动上送。

```ini
[global]
port=2404                       ; 监听端口
max_clients=10                  ; 最大并发客户端数
verbose=1
ai_upload_mode=change           ; AI 上送模式：change/cycle/both
ai_cycle_s=60                   ; AI 循环上送周期（秒）
spontaneous_with_timestamp=1    ; 主动上送是否带 CP56 时间戳
clock_sync_enable=1             ; 时钟同步响应
adjust_local_clock=0            ; 收到时钟同步后调整本机时间

; 指定监听 IP（可选，默认监听所有网口）
[listen_1]
ip=192.168.1.10                 ; 仅监听此 IP（生产环境建议指定）
port=2404

; 可配置多个 listen_N 绑定不同 IP:端口
; [listen_2]  ip=192.168.1.11  port=2405

[Device_1]
common_addr=1
desc=1号从站
di_001 = ioa=1000, ch=1, dev=1, point=2
ai_001 = ioa=2000, type=M_ME_NC_1, ch=1, dev=1, point=1, scale=1.0
do_001 = ioa=3000, val=0, ch=1, dev=1, point=1
do_002 = ioa=3000, val=1, ch=1, dev=1, point=2
```

**Global 配置字段：**

| 字段 | 说明 | 默认值 |
|------|------|--------|
| `port` | 监听端口 | 2404 |
| `max_clients` | 最大并发客户端数 | 10 |
| `verbose` | 日志级别 | 1 |
| `ai_upload_mode` | AI 主动上送模式 | change |
| `ai_cycle_s` | AI 循环上送周期（秒） | 60 |
| `spontaneous_with_timestamp` | 主动上送带 CP56 时间戳 | 1 |
| `clock_sync_enable` | 响应主站时钟同步 | 1 |
| `adjust_local_clock` | 同步时调整本机系统时间 | 0 |
| `t0` | 连接超时（秒） | 30 |
| `t1` | 发送/测试超时（秒） | 15 |
| `t2` | 接收确认延迟（秒） | 10 |
| `t3` | 测试帧发送间隔（秒） | 20 |
| `k` | 最大未确认 I 帧数 | 12 |
| `w` | 最大未确认接收帧数 | 8 |

**AI 上送模式说明：**

| 模式 | 行为 |
|------|------|
| `change` | 仅 AI 值变化时上送（默认） |
| `cycle` | 按 `ai_cycle_s` 周期循环上送全部 AI |
| `both` | 变化即上送 + 周期全量上送 |

**listen_N 配置（指定监听 IP）：**

默认监听所有网口（`0.0.0.0:2404`）。配置 `[listen_N]` 可限制监听特定 IP 地址，增强安全性：

```ini
; 仅监听内网 IP，拒绝外网连接
[listen_1]  ip=192.168.1.10  port=2404

; 多网口场景：同时监听内网和监控网
[listen_1]  ip=192.168.1.10  port=2404     ; 内网
[listen_2]  ip=10.0.0.10   port=2404     ; 监控网
```

未配置 `listen_N` 时等同于 `[listen_1] ip=0.0.0.0 port=2404`。

**Device 配置字段：**

| 字段 | 说明 |
|------|------|
| `common_addr` | 公共地址（ASDU 地址） |
| `desc` | 设备描述 |

**DI/AI/DO/AO 映射字段：**

| 字段 | 说明 |
|------|------|
| `ioa` | IEC 104 IOA 地址（主站寻址用） |
| `ch` | 本地通道号 |
| `dev` | 本地设备号 |
| `point` | 本地四遥点号 |
| `type` | AI 类型（M_ME_NA_1/M_ME_NC_1/M_ME_NB_1） |
| `scale` / `offset` | 工程转换 |
| `val` | DO 控制值（0=分闸/1=合闸） |

### 4.14 iec103_master.ini —— IEC 103 主站配置

继电保护设备采集，FUN(功能码) + INF(信息号) 标识数据。每个通道独立串口线程，
通过模板定义的 FUN+INF 组合轮询设备。

**配置字段说明：**

| 字段 | 说明 |
|------|------|
| `port_name` | 串口名称（物理串口或 `IP:PORT` TCP 串口服务器） |
| `baud` / `parity` | 串口参数 |
| `scan_ms` | 轮询间隔（毫秒） |
| `fun` / `inf` | IEC 103 功能码 + 信息号（十六进制） |
| `point` | 本地四遥点号 |
| `type` | AI 编码类型（`normalized` / `float`） |
| `scale` / `offset` | 工程转换（`工程值 = 原始值 × scale + offset`） |
| `station` | 链路地址 |

**完整示例：**

```ini
[global]
timeout_ms=1000
verbose=1

[Channel_1]
port_name=COM1
baud=9600
parity=even
data_bits=8
stop_bits=1
scan_ms=3000
timeout_ms=1000

; === 模板定义 ===
[Template_保护装置]
; DI 映射
di_001 = fun=0x01, inf=0x01, point=2        ; 保护动作信号
di_002 = fun=0x01, inf=0x02, point=3        ; 重合闸信号
di_003 = fun=0x01, inf=0x03, point=4        ; 告警信号
; AI 映射
ai_001 = fun=0x02, inf=0x01, type=float,  point=1, scale=0.1       ; A相电流
ai_002 = fun=0x02, inf=0x02, type=float,  point=2, scale=0.1       ; B相电流
ai_003 = fun=0x02, inf=0x03, type=float,  point=3, scale=0.1       ; C相电流
ai_004 = fun=0x02, inf=0x04, type=float,  point=4, scale=0.01      ; A相电压

[Template_变压器]
ai_001 = fun=0x02, inf=0x01, type=normalized, point=1, scale=0.01  ; 上层油温
ai_002 = fun=0x02, inf=0x02, type=normalized, point=2, scale=1.0   ; 档位

; === 设备绑定 ===
[Device_1]
channel=1
station=1
desc=线路保护柜
template=保护装置

[Device_2]
channel=1
station=2
desc=主变保护
template=变压器
```

**注意：** 当前响应解析为简化实现（取最后一个非终止字节作 rawVal），非标准 IEC 103 ASDU 结构。
如需与严格规约从站互通，需升级应答解析逻辑（见 `PollDevice` 中 `TODO(103-2)` 注释）。

---

### 4.15 iec101_master.ini —— IEC 101 主站配置

串口 IEC 101 主站采集，每通道独立线程，ASDU 层复用 IEC 104 的 IecType / IecCOT 定义。

**配置字段说明：**

| 字段 | 说明 |
|------|------|
| `port_name` | 串口名称 |
| `link_addr` | 链路层地址（1-65535） |
| `common_addr` | 公共地址（1-65535） |
| `ioa` | ASDU 层信息体地址 |
| `gi_cycle_s` | 总召唤周期（秒），0=禁用 |

**完整示例：**

```ini
[global]
timeout_ms=1000
verbose=1

[Channel_1]
port_name=COM1
baud=9600
parity=even
data_bits=8
stop_bits=1
timeout_ms=1000
gi_cycle_s=300          ; 5 分钟召唤一次全数据

[Channel_2]
port_name=192.168.1.10:2404
baud=9600
parity=even
timeout_ms=1000
gi_cycle_s=300

[Device_1]
channel=1
link_addr=1
common_addr=1
desc=远动装置1
; DI 映射
di_001 = ioa=1000, point=2
di_002 = ioa=1001, point=3
; AI 映射
ai_001 = ioa=2000, point=1, type=M_ME_NC_1, scale=0.01
ai_002 = ioa=2001, point=2, type=M_ME_NA_1, scale=0.1

[Device_2]
channel=2
link_addr=2
common_addr=2
desc=远动装置2
di_001 = ioa=1000, point=4
ai_001 = ioa=2000, point=3, type=M_ME_NC_1, scale=1.0
```

**工作流程：** 启动后发送总召唤（GI）请求 → 等待从站 GI 响应 → 循环按 `gi_cycle_s`
周期召唤。收到从站自发变位帧（COT=3）时触发 HandleSpontaneous 更新本地点表。

---

### 4.16 iec101_slave.ini —— IEC 101 从站配置

串口 IEC 101 从站，响应主站 GI 请求 + 遥控命令。

**配置字段说明：**

| 字段 | 说明 |
|------|------|
| `link_addr` | 本设备链路地址（1-65535，>255 时需与主站约定 2 字节寻址） |
| `common_addr` | 公共地址 |
| `ioa` | ASDU 信息体地址（主站通过 IOA 寻址） |

**完整示例：**

```ini
[global]
port_name=COM1
baud=9600
parity=even
data_bits=8
stop_bits=1
verbose=1

[Device_1]
link_addr=1
common_addr=1
desc=1号远动终端

; DI 映射：从本地点表读遥信值，主站 GI 时上送
di_001 = ioa=1000, ch=1, dev=1, point=2
di_002 = ioa=1001, ch=1, dev=1, point=3
di_003 = ioa=1002, ch=1, dev=1, point=4

; AI 映射：上送本地遥测
ai_001 = ioa=2000, ch=1, dev=1, point=1, type=M_ME_NC_1, scale=1.0

; DO 映射：收到遥控指令后写 SetDoMaster
do_001 = ioa=3000, val=1, ch=1, dev=1, point=1    ; 合闸
do_002 = ioa=3000, val=0, ch=1, dev=1, point=1    ; 分闸

[Device_2]
link_addr=2
common_addr=2
desc=2号远动终端
di_001 = ioa=1000, ch=2, dev=1, point=2
ai_001 = ioa=2000, ch=2, dev=1, point=1, type=M_ME_NC_1
```

**帧格式说明：** 固定帧（复位/ACK = `10 CTRL ADDR_L ADDR_H CS 16`）为 6 字节，
`link_addr` 按小端写入 `ADDR_L` 与 `ADDR_H`。可变帧（GI 响应等）LEN 字段 =
`3 + asduLen`（不含起始符和 CS）。

---

### 4.17 dlt645_master.ini —— DLT 645 电表配置

DL/T 645-1997/2007 电表采集，RS-485 串口，BCD 编码数据。

**协议要点：**（第二轮 DLT-1/2/3 修复）
- DATA 段（DI + 值）发送前每字节 +0x33，接收后 -0x33
- CS 校验和从第一个起始符 0x68 起累加（含起始符）
- 响应入口三重验证：CS 正确、地址回比一致、DI 与请求一致

**配置字段说明：**

| 字段 | 说明 |
|------|------|
| `address` | 电表 6 字节 BCD 地址（低字节先送，如 `010001234567`） |
| `data_id` | 数据标识：1997=2 字节（如 `0000`），2007=4 字节（如 `00000000`） |
| `template` | `1997` 或 `2007`，决定 DI 长度 |

**完整示例：**

```ini
[global]
timeout_ms=1000
verbose=1
baud=2400
retry_count=2
retry_sleep_ms=5000

[Channel_1]
port_name=COM1
baud=2400
parity=even
data_bits=8
stop_bits=1
scan_ms=10000
timeout_ms=1000

; === 1997 模板（2 字节数据标识） ===
[Template_1997]
; 数据标识 4 位 HEX，data_id = DI0|DI1
ai_001 = data_id=0000, point=1, scale=0.01     ; 组合有功总电能 (kWh)
ai_002 = data_id=0101, point=2, scale=0.1      ; A相电压 (V)
ai_003 = data_id=0102, point=3, scale=0.1      ; B相电压 (V)
ai_004 = data_id=0103, point=4, scale=0.1      ; C相电压 (V)
ai_005 = data_id=0201, point=5, scale=0.001    ; A相电流 (A)
ai_006 = data_id=0301, point=6, scale=0.001    ; 有功功率 (kW)
ai_007 = data_id=0601, point=7, scale=0.001    ; 频率 (Hz)

; === 2007 模板（4 字节数据标识） ===
[Template_2007]
; 数据标识 8 位 HEX，data_id = DI0|DI1|DI2|DI3
ai_001 = data_id=00000000, point=1, scale=0.01 ; 组合有功总电能
ai_002 = data_id=02010100, point=2, scale=0.1  ; A相电压
ai_003 = data_id=02010200, point=3, scale=0.1  ; B相电压
ai_004 = data_id=02010300, point=4, scale=0.1  ; C相电压
ai_005 = data_id=02020100, point=5, scale=0.001; A相电流
ai_006 = data_id=02030100, point=6, scale=0.001; 有功功率
ai_007 = data_id=02060000, point=7, scale=0.001; 频率

; === 设备绑定 ===
[Device_1]
channel=1
address=010001234567
template=2007
desc=1号电度表
baud=2400

[Device_2]
channel=1
address=010005432100
template=1997
desc=2号电度表
baud=2400
```

**注意：** `address` 是 12 位 BCD 字符串，发送时按"低字节先送"顺序排入帧。
例如地址 `010001234567` 映射到帧内 6 字节为 `67 45 23 01 00 01`。

---

### 4.18 cdt_master.ini —— CDT 主站配置

被动接收 RTU 循环上送的 CDT（循环式远动）帧。系统作为主站等待
RTU 定期上送 YC（遥测）、YX（遥信）、YM（电度）帧。

```ini
[global]
timeout_ms=1000
verbose=1

[Channel_1]
port_name=COM1
baud=1200
parity=even
data_bits=8
stop_bits=1
timeout_ms=1000

[Device_1]
channel=1
station=1
; YC (遥测) → AI：addr 对应 CDT 帧内的遥测通道号
yc_001 = addr=0,  point=1, scale=0.1
yc_002 = addr=1,  point=2, scale=0.1
yc_003 = addr=2,  point=3, scale=0.01
; YX (遥信) → DI：addr 对应帧内 16 位遥信字，bit 选位
yx_001 = addr=0,  bit=0,  point=4        ; 第1路遥信
yx_002 = addr=0,  bit=1,  point=5        ; 第2路遥信
yx_003 = addr=1,  bit=0,  point=6        ; 第13路遥信
; YM (电度) → AI：YM 帧 0x60 上送
ym_001 = addr=0,  point=7,  scale=0.01
ym_002 = addr=1,  point=8,  scale=0.01

[Device_2]
channel=1
station=2
yc_001 = addr=0, point=10, scale=1.0
yx_001 = addr=0, bit=0,   point=11
```

**CDT 帧结构说明：**

| 字节 | 内容 |
|------|------|
| 0-5 | 同步头（3× EB 90） |
| 6 | 功能码（YC=0x61, YX=0x71, YM=0x60） |
| 7 | infoCount \| (station << 3) |
| 8+ | 数据字段 |
| 末尾 | CS（从字节 6 起求和 mod 256） |

---

### 4.19 cdt_slave.ini —— CDT 从站配置

主动循环上送 CDT 帧。系统作为 RTU 从站，通过串口定期向 CDT 主站
发送遥测、遥信、电度数据。

```ini
[global]
port_name=COM1
baud=1200
parity=even
data_bits=8
stop_bits=1
cycle_ms=1000          ; 上送周期（毫秒）

[Device_1]
station=1
; YC 遥测：从本地 AI 点表取值
yc_001 = addr=0, ch=1, dev=1, point=1, scale=0.1
yc_002 = addr=1, ch=1, dev=1, point=2, scale=0.01
; YX 遥信：从本地 DI 点表取值，16 位遥信字
yx_001 = addr=0, bit=0, ch=1, dev=1, point=3
yx_002 = addr=0, bit=1, ch=1, dev=1, point=4
; YM 电度：从本地 AI 点表读电度值
ym_001 = addr=0, ch=1, dev=1, point=5, scale=1.0
; DO 映射：接收 CDT 主站遥控命令
do_001 = addr=0, val=1, ch=1, dev=1, point=6
do_002 = addr=0, val=0, ch=1, dev=1, point=6

[Device_2]
station=2
yc_001 = addr=0, ch=2, dev=1, point=10, scale=1.0
yx_001 = addr=0, bit=0, ch=2, dev=1, point=11
```

**CDT 从站发送帧序列：** 每 `cycle_ms` 毫秒循环发送：
1. YC 帧（0x61）— 32 位浮点遥测值
2. YX 帧（0x71）— 每组 16 位遥信
3. YM 帧（0x60）— 32 位整数电度值（带 infoCount + 站号字节，H11 修复）

---

### 4.20 CommIO —— 串口/TCP 统一 IO 说明

CommIO 是系统底层的统一 IO 封装，自动判断使用物理串口还是 TCP 串口服务器，所有串口模块（Modbus RTU、IEC 101/103、DLT 645、CDT）均通过它进行通讯。

**自动判断规则：**

| `port_name` 格式 | 判断 | 示例 |
|------------------|------|------|
| `COM1` ~ `COM256` | 物理串口（Windows） | `COM1`、`COM3` |
| `/dev/ttyS*`、`/dev/ttyUSB*` | 物理串口（Linux） | `/dev/ttyS0`、`/dev/ttyUSB0` |
| `/dev/tty.*` | 物理串口（macOS） | `/dev/tty.usbserial-*` |
| `IP:PORT`（含 `:` 且无 `/` 或 `\`） | TCP 串口服务器 | `192.168.1.100:10000`、`10.0.0.5:2001` |

**典型场景示例：**

**场景一：物理串口直连 RS-485 设备**

```ini
[Channel_1]
port_name=COM1              ; 工控机物理串口
baud=9600
parity=even
data_bits=8
stop_bits=1
scan_ms=3000
```

**场景二：通过 TCP 串口服务器连接远端设备**

串口服务器（如 USR-N510、有人物联网）将 RS-485/232 转为 TCP/IP，PMPC 通过 TCP 连接串口服务器即可读写串口设备，无需物理串口线。

```ini
[Channel_1]
port_name=192.168.1.200:10000   ; 串口服务器 IP:端口
baud=9600
parity=even
data_bits=8
stop_bits=1
scan_ms=3000
```

**场景三：混合组网——多通道同时使用物理串口和 TCP 串口服务器**

```ini
[Channel_1]
port_name=COM1                   ; 通道1：本地物理串口（RS-485 电表总线）
baud=9600
parity=even
scan_ms=2000

[Channel_2]
port_name=192.168.1.200:10000    ; 通道2：TCP 串口服务器（远端保护装置）
baud=19200
parity=even
scan_ms=1000

[Channel_3]
port_name=192.168.1.201:10000    ; 通道3：另一台串口服务器（不同子站）
baud=9600
parity=odd
scan_ms=3000
```

> **注意：** TCP 串口服务器模式下，PMPC 作为 TCP 客户端主动连接串口服务器的监听端口。串口服务器的波特率/校验位等串口参数需与 PMPC 配置一致。连接断开后自动重连（间隔由 `retry_sleep_ms` 控制）。

---

## 5. 模块详解

### 5.1 核心数据服务 (RemoteDataMgr)

**全局单例**（C++11 线程安全的局部 static），管理所有四遥数据。

数据结构：

```
channels_[] → devList[] → diList[] / aiList[] / doList[] / aoList[]
```

**锁策略：**

- `structMtx_` —— 仅保护 `channels_` 向量结构（查找时微秒级持有）
- `Device::devMtx` —— 每设备独立 `unique_ptr<mutex>`，设备之间完全无锁竞争
- 同一通道不同设备的读写完全并行

**DI 点号 1 约定：**

- 点号 1 固定为每个设备的通讯状态指示
- `modbus_tcp_master` 在连接成功/恢复时写 1（ON=在线），通讯中断时写 0（OFF=离线）
- `change=false` 确保不产生 SOE 事件
- `CheckAllPointChange` 循环跳过 `pointNo == 1`，不上送时标、不打印变化

**关键接口：**

| 接口 | 说明 |
|------|------|
| `SetDi(ch, dev, pt, val, ts, change)` | 写遥信 |
| `SetAi(ch, dev, pt, val)` | 写遥测 |
| `SetDoMaster(ch, dev, pt, val)` / `SetDoSlave` | 写遥控 |
| `SetAo(ch, dev, pt, val)` | 写遥调 |
| `GetDi/Ai/Do/Ao(ch, dev, pt, &out)` | 读单点 |
| `GetDiList/AiList(ch, dev, &out)` | 读全部列表 |
| `GetChannelIds()` / `GetDeviceIds(ch)` | 遍历通道/设备 |
| `CheckAllPointChange()` | 主循环 200ms 检测点位变化并打印 |

---

### 5.2 Modbus TCP 主站 (modbus_tcp_master)

驱动每通道独立线程，自动连接/断开/重连。

**采集流程：**

```
ChannelThread → connect → PollDevice → WriteDOChanges → sleep(scanMs)
```

**PollDevice 逻辑：**

1. 收集所有 DI/AI 条目，按功能码分组
2. FC01/02 发送读取请求，按位解析写入本地 DI
3. FC03/04 按地址排序，合并连续地址块，每块发一次请求，按 point 分发

**通讯状态管理：**

- 连接成功时：`commStatus_[key] = true`，`SetDi(ch, dev, 1, true, ts, false)`
- 采集循环数据正常时：`commStatus_[key] = true`，`SetDi(ch, dev, 1, true, ts, false)`
- 通讯中断时：`commStatus_[key] = false`，`SetDi(ch, dev, 1, false, ts, false)`
- 重试用尽时：标记离线 + 写点号 1 = 0

**断线重连：**

```
socket_error → 标记离线 → retry_count 次重试 → 全部失败 →
sleep(retry_sleep_ms) → 重新开始重试
```

**地址合并优化示例：**

`42 个连续地址的 AI 条目自动合并为 1 次 FC03 请求(qty=42)`，收到 84 字节后按 point 分发。

---

### 5.3 Modbus TCP 从站 (modbus_tcp_slave)

监听 TCP 端口，接收 Modbus 主站请求，从本地 `RemoteDataMgr` 读取/写入数据。

**功能码处理：**

| 功能码 | 处理方式 | 数据源 |
|--------|----------|--------|
| 01 读线圈 | 按位打包 DI | `RemoteDataMgr::GetDi()` |
| 02 读离散输入 | 按位打包 DI | `RemoteDataMgr::GetDi()` |
| 03 读保持寄存器 | 从 AI Map 和 DI Map 读取 | AI 直读 + DI 按位叠加 |
| 04 读输入寄存器 | 同上 | 同上 |
| 05 写单个线圈 | 写入 DO | `RemoteDataMgr::SetDoMaster()` |

**FC03/04 的 AI + DI 共存机制：**

1. 先按 AI 映射填充寄存器值（含 dtype/scale/offset 转换）
2. 再按 DI 映射（按位）叠加到已有寄存器值
3. 同一寄存器可同时承载 AI 数据和 DI 按位遥信

**点对点映射：**

每个 `addr` 独立映射到 `(ch, dev, point)`，地址可以不连续，可跨通道、跨设备。

```ini
; 同一 Device 下的条目可映射到不同通道/设备
fc01_001 = addr=0,  ch=1, dev=1, point=2    → 通道1 设备1
fc01_002 = addr=10, ch=2, dev=1, point=2    → 通道2 设备1
```

---

### 5.4 PEMP2.0 遥测推送 (pemp_server)

实现 PEMP2.0 通讯协议服务端，通讯管理机为服务端，后台为客户端。

**消息循环结构：**

```
client_handler:
  1. try_recv 后台命令（500ms 超时）
  2. 收到命令 → handle_request → 处理并回复
  3. 超时 → 主动上传模式(ACTIVE)下检查 DI/AI 上传间隔
  4. 一发一答约束：发送主动数据后必须等 ACK 或超时(1500ms)

主动上传定时器:
  lastDiUpload —— DI(51H) 按 di_upload_ms 间隔
  lastAiUpload —— AI(53H) 按 ai_upload_ms 间隔
  SOE(52H) —— 待发送数据 > 0 时立即上传
```

**主动上传内容：**

- **51H 上传遥信** —— 遍历所有通道/设备，按位打包 DI，每设备一帧
- **53H 上传遥测** —— 遍历所有通道/设备，AI 值打包为 int32 数组，每设备一帧
- **52H 上传 SOE** —— 调用 `g_soeQueue.PopAll()` 取所有待发送 SOE 事件

---

### 5.5 调试控制台 (debug_console)

TCP 调试控制台，通过 telnet 连接进行交互式调试。

**连接方式：**

```bash
telnet 127.0.0.1 9090
```

连接后自动打印所有命令的使用说明。

**命令详细说明：**

1. **读取四遥值**

```
get di 1 1          显示通道1设备1的全部DI点
get di 1 1 5        显示通道1设备1的第5个DI点
get ai 1 1          显示全部AI
get ai 1 1 3        显示第3个AI点值
```

2. **设置四遥值**

```
set di 1 1 3 1      DI(1,1,3) = ON
set ai 1 1 2 23.45  AI(1,1,2) = 23.45
set do 1 1 1 0      DO(1,1,1) master = OFF
set ao 2 1 1 100.0  AO(2,1,1) = 100.0
```

3. **自动变位**

```
auto di 1 1 10 500    启动任务#1: DI(1,1,10) 每500ms翻转
auto ai 1 1 2 1000    启动任务#2: AI(1,1,2) 正弦波周期1000ms
auto stop 1           停止任务#1
auto stop all         停止全部
```

4. **角色切换**

```
role                  查看当前角色
role master           切换为主机模式
role standby          切换为备机模式
```

5. **模块控制**

```
status                查看所有模块运行状态
start tcp_server      启动模块
stop tcp_server       停止模块
reload modbus_cfg     重载主站配置
reload point          重载点表
```

6. **报文记录控制**

```
log start             启动报文记录（全部设备）
log start 1 1         仅记录通道1设备1
log status            查看记录状态
log parse off         关闭报文解析（仅 HEX）
log stop              停止记录
```

---

### 5.6 报文记录器 (packet_logger)

记录 Modbus 收发报文到文件。**设备级文件滚动**：每设备每天一个文件。

**日志文件路径：**

```
logs/traffic/ch{N}_dev{N}_YYYYMMDD.log
```

**写文件触发时机：**

- `modbus_tcp_master` 的 `BuildAndSend` 和 `RecvFrame` 后自动调用
- `modbus_tcp_slave` 收到请求和发送响应后自动调用
- 调用方只需 `PacketLogger::Instance().Log(...)`，无需手动拼解析字符串

**协议自动解析架构：**

```
PacketLogger.Log(TX/RX, ch, dev, station, func, data, len)
    │
    │  遍历已注册的 ProtocolParser 列表
    │
    ├── ModbusTcpParser (内建，默认可用)
    │   ├── CanParse → 总是 true
    │   └── Parse    → 解析 FC01~06/0F/10 的请求和响应
    │
    ├── ModbusRtuParser (将来可添加)
    ├── Iec104Parser (将来可添加)
    │
    └── 都不匹配 → "(未知协议)"
```

**扩展新协议：**

```cpp
class MyProtocolParser : public ProtocolParser {
    const char* ProtocolName() const override { return "MyProtocol"; }
    bool CanParse(const ParseContext& ctx) const override { /* 判断条件 */ }
    std::string Parse(const ParseContext& ctx) const override { /* 解析逻辑 */ }
};

// 注册
PacketLogger::Instance().RegisterParser(std::make_unique<MyProtocolParser>());
```

**注意事项：**

- 报文记录默认**不启用**，需通过调试控制台或代码手动启动
- 通过 `log start` 启动后，所有匹配设备的收发报文自动写入
- 通过 `log parse off` 可关闭解析，仅记录 HEX 数据

---

### 5.7 双机冗余 (redundancy)

双机热备，支持心跳检测 + 角色切换 + 数据同步 + DO 脉冲复位。

**角色定义：**

| 角色 | 说明 |
|------|------|
| Idle | 初始状态，正在协商 |
| Standby | 备机：不启动采集，接收 DI/AI/AO 同步（不接收 DO） |
| Master | 主机：正常采集，下发 DO 后自动脉冲复位 |

**心跳机制：**

- 每秒发送一次心跳帧（`7B 55 LEN... 7D`）
- 连续丢失 `missed_heartbeat_limit` 次判定对方离线
- 优先级高的机器优先成为 Master

**角色切换联动：**

- **Master → Standby**：停止 Modbus 采集，启动数据同步接收
- **Standby → Master**：启动 Modbus 采集，启动 DO 脉冲复位线程
- 联动通过 `ModuleManager.StartModule("modbus_tcp_master")` / `StopModule` 实现

**DO 脉冲复位：**

Master 模式下发 DO 后，按 `doPulseMs` 宽度自动复位（OFF），防止长时间合闸。

**单机主备网口（`standby_ip`）：**

modbus_tcp_master 支持单机内主备网口切换。配置 `standby_ip` 后，采集线程在主网口断线时自动切换到备网口，备线断开后根据 `fallback` 配置决定是否回切主线。详见 [4.3 主备网口说明](#43-modbus_tcp_masterini--modbus-tcp-主站采集配置)。

单机主备网口与双机冗余可叠加使用，互不冲突。

**采集模块主备配置：**

双机冗余场景下，两台机器的采集模块配置须注意以下差异：

| 配置项 | 主备差异说明 |
|--------|-------------|
| `modbus_tcp_master.ini` 的 `Channel_N`/`ip` | 两台机器连接同一批现场设备，IP/端口相同。若主备机分属不同网段，可在各自机器上配置不同的 `ip` |
| `modbus_rtu_master.ini` 的 `port_name` | 主备机的物理串口号可能不同（主机 COM1、备机 COM3），需分别配置 |
| `point_cfg.ini` 点表 | **完全相同**——两台机器共享同一套点表配置 |
| 其他采集模块（IEC 104/101、DLT 645、CDT） | 串口/TCP 参数规则同上 |

**典型双机组网配置示例：**

```
主机 A（IP=192.168.1.10，物理串口 COM1）           备机 B（IP=192.168.1.11，物理串口 COM3）
┌─────────────────────────────┐                  ┌─────────────────────────────┐
│ modbus_tcp_master.ini:      │                  │ modbus_tcp_master.ini:      │
│   [Channel_1]               │                  │   [Channel_1]               │
│   ip=192.168.1.100  ← 同一PLC                   ip=192.168.1.100  ← 同一PLC
│                            │                  │                            │
│ modbus_rtu_master.ini:      │                  │ modbus_rtu_master.ini:      │
│   [Channel_1]               │                  │   [Channel_1]               │
│   port_name=COM1     ← 串口不同                  port_name=COM3     ← 串口不同
│   baud=9600                 │                  │   baud=9600                 │
│                            │                  │                            │
│ redundancy.ini:             │                  │ redundancy.ini:             │
│   local_name=station_a      │                  │   local_name=station_b      │
│   peer_ip=192.168.1.11      │                  │   peer_ip=192.168.1.10      │
│   priority=200              │                  │   priority=100              │
│   heartbeat_port=7503       │                  │   heartbeat_port=7503       │
└─────────────────────────────┘                  └─────────────────────────────┘
```

> **注意：** `point_cfg.ini` 和 `app_modules.ini` 在两台机器上**完全一致**。采集模块配置文件（如 `modbus_tcp_master.ini`）若主备机串口/TCP 参数相同也可保持完全一致。差异仅通过 `redundancy.ini` 的 `local_name`/`peer_ip` 和各自机器的串口分配体现。

---

### 5.8 模块管理器与热重载

ModuleManager 统一管理所有 AppModule 的生命周期。

**启动流程：**

```
main() → modules.LoadConfig("app_modules.ini")
       → 解析各模块配置 → ModuleFactory::Create(name) → mod->LoadConfig(cfgFile)
       → modules.StartAll()
       → 每个模块 mod->Start()
       → 注册冗余联动回调（StartModule/StopModule 控制采集）
       → 启动 WatchLoop（文件监控）
```

**热重载流程：**

```
WatchLoop(每2秒) → stat(cfgFile) → HasChanged(oldStamp, newStamp)
 → 是 → Stop() → LoadConfig(cfgFile) → 校验错误
  → 不变 → Start() → OK
  → 错误 → 保持旧配置
```

**新增模块步骤：**

1. 创建 `.h` / `.cxx` 文件
2. 定义 `class XxxModule : public AppModule`，实现纯虚接口
3. 在 `.cxx` 末尾加 `REGISTER_MODULE("模块名", XxxModule)`
4. 在 `app_modules.ini` 中添加配置节
5. 在 `CMakeLists.txt` 和 `.vscode/tasks.json` 中添加源文件

---

### 5.9 MySQL 数据存储 (data_recorder)

订阅 EventBus 的四遥事件，将数据写入 MySQL 数据库，支持多台 PMPC 实例共享。

**写入路径：**

```
RemoteDataMgr::SetDi/SetAi/SetDo/SetAo
  │
  ├── EventBus::Publish(DIChange/AIChange/...)
  │
  ▼
DataRecorder (订阅 EventBus)
  ├── DI → di_log_YYYYMM + rt_status
  ├── AI → rt_status（实时值）
  ├── DO/AO → rt_status
  │
  └── TimerThread（每5秒）
       ├── mysql_ping 保活
       ├── 更新 pmpc_instance
       └── WriteAI → ai_log_YYYYMM（时间对齐批量写入）
```

**DI 记录：** EventBus 事件驱动，每次 SetDi 触发 INSERT
**AI 存盘：** 定时器驱动，按各点独立间隔写入，时间对齐去重
**实时值：** `rt_status` 表 UPSERT 更新

**月度表自动创建：** 每月首次写入时 `CREATE TABLE IF NOT EXISTS di_log_202607`，MySQL 原子操作保证多实例不冲突。

**版本兼容：** `schema_version` 表管理表结构版本，启动时检查，版本不匹配时打印警告降级运行。

---

### 5.10 Modbus RTU 主站 (modbus_rtu_master)

基于串口的 Modbus RTU 主站采集模块，与 TCP 主站共享相同的映射数据模型和模板继承机制。

**端口级线程：** 每个串口号独立一个采集线程，支持多设备共享同一串口（轮询采集）。

**通信协议：**

```
RTU 请求帧: [地址(1B)] [功能码(1B)] [数据(NB)] [CRC16(2B 小端)]
RTU 响应帧: [地址(1B)] [功能码(1B)] [数据(NB)] [CRC16(2B 小端)]
```

- CRC16 校验（Modbus 标准多项式 0xA001）
- 3.5 字符时间静默帧间隔

**通讯状态管理：** 串口打开/断开时自动更新 DI 点号 1，与 TCP 主站一致。

**支持功能码：** FC01(读线圈)、FC02(读离散输入)、FC03(读保持寄存器)、FC04(读输入寄存器)。地址合并优化逻辑与 TCP 主站相同。

---

### 5.11 Modbus RTU 从站 (modbus_rtu_slave)

串口 Modbus RTU 从站模块，配置格式与 `modbus_tcp_slave` 相同，底层使用 `CommIO` 支持物理串口和 TCP 串口服务器。

**帧处理：** 接收 RTU 请求帧 → CRC16 校验 → 查找 station_id → 功能码分发 → 构建 RTU 响应帧 → CRC16 发送

**支持功能码：** FC01(读线圈)、FC02(读离散输入)、FC03(读保持寄存器)、FC04(读输入寄存器)、FC05(写单个线圈)。

---

### 5.12 IEC 104 主站 (iec104_master)

IEC 60870-5-104 主站采集模块，长连接 + 总召唤 + 主动上报。

**通道级线程：** 每个 TCP 连接独立线程，自动断线重连。

**采集流程：** TCP 连接 → STARTDT 握手 → 总召唤 GI → 定时总召唤刷新 → 接收主动上报

**ASDU 类型：** 支持 M_SP_NA_1(单点遥信)、M_SP_TB_1(带时标遥信)、M_ME_NC_1(浮点遥测)、M_ME_NA_1(归一化值)、M_IT_NA_1(电度)。遥控 C_SC_NA_1/C_DC_NA_1，遥调 C_SE_NC_1。

**配置：** `global → Channel → Template → Device`，IOA 寻址，`type=` 指定 ASDU 类型。

---

### 5.13 IEC 104 从站 (iec104_slave)

IEC 60870-5-104 从站模块，响应主站请求。

**功能：** GI 总召唤响应（发送全量 DI/AI）、遥控执行（C_SC_NA_1 → SetDoMaster）、遥调执行（C_SE_NC_1 → SetAo）、DI 变化主动上送（EventBus → M_SP_TB_1）、AI 三模式上送（change/cycle/both）、时钟同步（C_CS_NA_1）。

**AI 上送模式：** `change`=变化上送、`cycle`=循环上送、`both`=变化+循环。`ai_cycle_s` 配置循环间隔。

---

### 5.14 IEC 103 主站 (iec103_master)

IEC 60870-5-103 主站，用于继电保护设备采集。数据通过 FUN(功能码) + INF(信息号) 标识。

**帧格式：** `68 LEN LEN 68 CTRL ADDR ASDU CS 16`

**支持类型：** `type=normalized`(16位归一化值) / `type=float`(32位 IEEE 754)。

---

### 5.15 IEC 101 主站 (iec101_master)

IEC 60870-5-101 串口主站。ASDU 层与 IEC 104 完全相同，链路层使用可变帧(`68 LEN LEN 68 ... CS 16`)和固定帧(`10 CMD ADDR CS 16`)。使用 `CommIO` 支持物理串口和 TCP 串口服务器。

**采集流程：** 打开串口 → 复位链路 → 发送总召唤 GI → 请求一级数据 → 定时重复。

---

### 5.16 IEC 101 从站 (iec101_slave)

IEC 60870-5-101 串口从站。监听串口接收主站请求，响应总召唤 GI（发送 DI 全量）、接收遥控命令（C_SC_NA_1 → SetDoMaster）。

---

### 5.17 DLT 645 电表采集 (dlt645_master)

DLT 645 电表采集模块，支持 1997 和 2007 两种协议版本。使用 RS-485 串口，`CommIO` 统一 IO。

**帧格式：** `68 A0..A5 68 C L DATA CS 16`

**BCD 编码：** 地址为 12 位 BCD 码，数据为 BCD 编码（低字节在前），通过 scale/offset 转换。

**模板化配置：** `Template_1997`（2 字节 DI）和 `Template_2007`（4 字节 DI）。设备引用模板自动继承所有数据映射。遥脉统一按 AI 存储。

**波特率动态切换：** 各设备可独立设置波特率，采集时自动切换。

---

### 5.18 CDT 主站 (cdt_master)

CDT（循环式远动规约，DL/T 634）主站，被动接收 RTU 循环上送的帧。

**帧格式：** 同步头 `EB 90 EB 90 EB 90` + 控制字 + 数据 + CRC

**功能码：** `0x01~0x03`=YC(遥测)、`0x10~`=YX(遥信)、`0x60~0x63`=YM(电能)。

**映射：** YC → AI（scale/offset 可配）、YX(addr,bit) → DI、YM → AI。

---

### 5.19 CDT 从站 (cdt_slave)

CDT 从站，主动循环上送 CDT 帧。读取本地四遥值，按 CDT 帧格式打包通过串口发送。

**发送周期：** `cycle_ms` 配置发送间隔。

**数据：** YC（读取 AI → 2 字节定点数）、YX（读取 DI → 按位打包）、YM（读取 AI → 4 字节）。

---

## 6. PEMP2.0 通讯协议

基于 PEMP2.0 后台通讯协议（版本 2.10），通讯管理机为服务端，后台为客户端。

**传输层：**

- 协议：TCP
- 默认端口：4096
- 通讯管理机 = 服务端，后台 = 客户端

**帧格式：**

```
7BH | FUN | LEN(2B 小端) | DATA | 7DH
```

- 帧头：`0x7B`
- 帧尾：`0x7D`
- 长度：2 字节小端，LEN = DATA 区字节数
- 总长 = LEN + 5（帧头 + FUN + 长度2 + 帧尾）

**字节序：** 小端（低位在前）

**约束：** 一发一答，发送后需等回复或超时才能发下一条

**功能码列表：**

| 功能码 | 方向 | 说明 |
|--------|------|------|
| 01H | 命令 | 查询状态 |
| 02H | 命令 | 切换工作状态 |
| 03H | 命令 | 召唤遥测 |
| 04H | 命令 | 召唤历史 SOE |
| 05H | 命令 | 召唤参数（未实现） |
| 06H | 命令 | 执行遥控 |
| 07H | 命令 | 下载参数（未实现） |
| 08H | 命令 | 同步时钟 |
| 09H | 命令 | 召唤遥信 |
| 51H | 上传 | 上传遥信（主动推送） |
| 52H | 上传 | 上传 SOE（主动推送） |
| 53H | 上传 | 上传遥测（主动推送） |
| 55H | 响应 | 异常帧 |

**帧结构示例：**

```
查询状态 (01H):
  7B 01 00 00 7D

切换主动上传 (02H):
  7B 02 01 00 03 7D    // 03=启动主动上传

召唤遥测 (03H):
  7B 03 02 00 CH DEV 7D

召唤遥信 (09H):
  7B 09 02 00 CH DEV 7D

上传遥测 (53H):
  7B 53 LEN_L LEN_H CH DEV N_L N_H AI1_L AI1_H ... 7D

上传遥信 (51H):
  7B 51 LEN_L LEN_H CH DEV N_L N_H packedBytes... 7D

异常帧 (55H):
  7B 55 02 00 ORIG_FUN ERR_CODE 7D
```

**工作状态字节（01H 查询回复）：**

| bit | 含义 |
|-----|------|
| bit0 (0x01) | 自检正常 |
| bit1 (0x02) | 主动上传中 |
| bit2 (0x04) | 主机 |

详细功能码及帧结构请参阅 `PEMP2.0通讯协议.doc`。

---

## 7. 编译与运行

### 7.1 MinGW-W64（Windows）

```powershell
# Debug 构建
g++ -std=c++17 -I include -I"C:/Program Files/MySQL/MySQL Server 8.4/include" ^
    -Wall -Wextra -Wpedantic -Wconversion -pthread -g -O0 ^
    -L"C:/Program Files/MySQL/MySQL Server 8.4/lib" ^
    src/main.cxx src/pmpc_config_reader.cxx src/pmpc_data_mgr.cxx ^
    src/pemp_server.cxx src/protocol.cxx src/soe_queue.cxx ^
    src/modbus_tcp_master.cxx src/modbus_tcp_slave.cxx ^
    src/modbus_rtu_master.cxx src/modbus_rtu_slave.cxx ^
    src/iec104_master.cxx src/iec104_slave.cxx ^
    src/iec103_master.cxx src/iec101_master.cxx src/iec101_slave.cxx ^
    src/dlt645_master.cxx src/cdt_master.cxx src/cdt_slave.cxx ^
    src/debug_console.cxx src/data_recorder.cxx src/packet_logger.cxx ^
    src/module_manager.cxx src/redundancy.cxx src/ini_reader.cxx ^
    src/socket.cpp src/serial_port.cpp ^
    -o pmpd.exe -llibmysql -lws2_32

# Release 构建
g++ -std=c++17 -I include -I"C:/Program Files/MySQL/MySQL Server 8.4/include" ^
    -Wall -Wextra -Wpedantic -Wconversion -pthread -O2 ^
    -L"C:/Program Files/MySQL/MySQL Server 8.4/lib" ^
    src/main.cxx src/pmpc_config_reader.cxx src/pmpc_data_mgr.cxx ^
    src/pemp_server.cxx src/protocol.cxx src/soe_queue.cxx ^
    src/modbus_tcp_master.cxx src/modbus_tcp_slave.cxx ^
    src/modbus_rtu_master.cxx src/modbus_rtu_slave.cxx ^
    src/iec104_master.cxx src/iec104_slave.cxx ^
    src/iec103_master.cxx src/iec101_master.cxx src/iec101_slave.cxx ^
    src/dlt645_master.cxx src/cdt_master.cxx src/cdt_slave.cxx ^
    src/debug_console.cxx src/data_recorder.cxx src/packet_logger.cxx ^
    src/module_manager.cxx src/redundancy.cxx src/ini_reader.cxx ^
    src/socket.cpp src/serial_port.cpp ^
    -o pmpc.exe -llibmysql -lws2_32
```

### 7.2 Linux / macOS

```bash
g++ -std=c++17 -I include -Wall -Wextra -Wpedantic -Wconversion -pthread -O2 \
    src/*.cxx src/socket.cpp src/serial_port.cpp -o pmpc -lmysqlclient
```

### 7.3 CMake（跨平台推荐）

```bash
cmake -S . -B build/release -DCMAKE_BUILD_TYPE=Release
cmake --build build/release
```

### 7.4 VS Code

| 快捷键 | 操作 |
|--------|------|
| `Ctrl+Shift+B` | 默认构建（g++ Release） |
| `F5` | 调试运行（g++ Debug + GDB） |

VS Code 已预配置以下 tasks（`tasks.json`）：

- `build-g++` —— Release 构建
- `build-g++-debug` —— Debug 构建（F5 使用）
- `cmake-release` / `cmake-debug` —— CMake 构建
- `run` —— 构建并运行
- `clean` —— 清理产物

### 7.5 运行

```bash
# 默认加载当前目录的 point_cfg.ini
pmpc.exe

# 指定点表路径
pmpc.exe point_cfg.ini
```

**运行要求：**

- 所有 `.ini` 配置文件需在**工作目录**下
- Modbus Master 需目标 Modbus 服务器可达
- 调试控制台默认端口 9090
- Windows 需要 `ws2_32` 库和 MySQL Connector/C（`libmysql.dll`）
- Linux/macOS 需要 pthread 库和 `libmysqlclient`（`apt install libmysqlclient-dev`）
- MySQL 8.4+（`pmpc_data` 数据库需提前创建，或由 `data_recorder` 模块自动创建）

**运行示例输出：**

```
========================================
  PMPC 四遥数据管理系统 v2.0
  动态模块 + 双机冗余
========================================
  Ctrl+C 退出 | 修改.ini自动热重载
========================================

加载点表: point_cfg.ini
点表加载完成

[DataRecorder] 配置加载: source=192.168.1.100 DI=3 AI=6 DO=1 AO=1
[ModbusTcpSlave] 配置加载完成: port=503 设备数=2
[PacketLogger] 配置加载: dir=logs/traffic parse=on
[PempServer] 监听端口 4096
[DebugConsole] 调试控制台启动: port=9090
[DataRecorder] MySQL 已连接
[DataRecorder] 模块启动完成
[ModuleManager] Loaded: 5/17 modules（按 enable=1 计算）

主循环开始...
```

**调试控制台连接：**

```bash
telnet 127.0.0.1 9090
```

---

## 8. 常见问题

**Q: 程序启动后无 Modbus 采集输出？**

A: 检查 `modbus_tcp_master.ini` 中的 ip/port，确认目标 Modbus Server 运行中。通过 `status` 命令查看模块运行状态。将配置文件 `verbose=2, hex_dump=1` 查看报文详情，或连接调试控制台执行 `log start` 启动报文记录查看原始收发数据。

**Q: 如何在运行时修改采集配置？**

A: 直接编辑对应的 `.ini` 文件并保存，系统 2 秒内自动检测到文件变化，执行热重载（`Stop → LoadConfig → Start`），无需重启程序。

**Q: 如何查看通讯报文？**

A: 连接调试控制台（`telnet 127.0.0.1 9090`），执行 `log start` 启动报文记录，日志文件保存在 `logs/traffic/` 目录，按通道/设备和日期分文件。执行 `log parse off` 可仅记录 HEX 不解析。

**Q: 如何手动设置四遥值进行测试？**

A: 连接调试控制台，使用 `set di/ai/do/ao` 命令设置任意点值。例如：

```
set ai 1 1 1 23.45
set di 1 1 3 1
```

也可使用 `auto di 1 1 10 500` 启动 DI 自动翻转，或 `auto ai 1 1 2 1000` 启动 AI 正弦波变位，模拟采集场景。

**Q: DI 点号 1 的通讯状态是什么意思？**

A: 点号 1 固定为每个设备的通讯状态指示，**ON (1) = 设备在线通讯正常**，**OFF (0) = 设备通讯中断**。由 `modbus_tcp_master` 模块在连接成功/恢复/中断时自动维护。业务遥信从**点号 2** 开始配置，`point_cfg.ini` 中的 `diCnt` 为业务遥信数量。

**Q: 如何查看设备是否在线？**

A: 三种方式：

1. 连接调试控制台，执行 `get di 1 1 1` 查看通讯状态点
2. 通过 PEMP2.0 协议召唤遥信（09H），第 1 位即为通讯状态
3. 直接查看日志中 `[ModbusTcpMaster] 设备 X 恢复通讯` 或 `[ModbusTcpMaster] 通讯中断` 输出

**Q: Windows 控制台中文乱码？**

A: 程序启动时自动调用 `SetConsoleOutputCP(CP_UTF8)`。VS Code 集成终端已在 `settings.json` 中配置 UTF-8 编码。如仍有乱码，确保终端字体支持中文（如 Consolas、YaHei Consolas Hybrid）。

**Q: 配置文件错误在哪里查看？**

A: 保存在 `logs/config_error_YYYYMMDD_HHMMSS.log` 文件中，包含错误所在行号和详细描述。程序启动时也会打印到 stderr。

**Q: 双机冗余如何测试？**

A: 单机场景：两台机器都配 `peer_ip=127.0.0.1`，分别用不同的 `local_name` 和 `priority` 配置，启动后观察角色协商结果。通过调试控制台 `role master`/`role standby` 命令手动切换角色，验证采集启停联动是否正常。

**Q: Modbus RTU 无法打开串口？**

A: 检查 `modbus_rtu.ini` 中的 `port_name` 是否与系统实际的串口号一致（Windows 为 `COM1` / `COM3`，Linux 为 `/dev/ttyS0` / `/dev/ttyUSB0`）。确认串口未被其他程序占用，波特率/校验位等参数与对端设备一致。模块默认 `enable=0`，需在 `app_modules.ini` 中改为 `enable=1`。

**Q: 如何添加新的采集模块？**

A: 1. 创建 `class XxxModule : public AppModule`，实现 Name/LoadConfig/ValidateConfig/Start/Stop/IsRunning 六个纯虚方法
2. 在实现文件的末尾加 `REGISTER_MODULE("模块名", XxxModule)` 宏
3. 在 `app_modules.ini` 中添加配置节（enable/cfg_file/auto_reload）
4. 在 `CMakeLists.txt` 和 `.vscode/tasks.json` 中添加源文件
5. 模块的 `Name()` 返回值需与 `REGISTER_MODULE` 的第一个参数一致

**Q: `build-g++` 任务卡住不动？**

A: 这是由于 `settings.json` 中 PowerShell 配置了 `-NoExit` 参数，导致 `"type": "shell"` 的 task 执行完后 PowerShell 不退出。解决方法是将 task 的 `"type"` 改为 `"process"`，或添加 `"options": {"shell": {"executable": "cmd.exe", "args": ["/d", "/c"]}}` 显式指定 cmd.exe 作为执行 shell。

**Q: IEC 101 和 IEC 104 有什么区别？**

A: IEC 104 基于 TCP/IP，IEC 101 基于 RS-485 串口。两者的 ASDU 层（数据类型、IOA 寻址、COT、GI 机制）完全相同，区别仅在于链路层：104 用 APCI（0x68+Len+4B Ctrl），101 用可变帧/固定帧 + CS 校验。IEC 101 主站模块直接复用 104 的 ASDU 处理逻辑。

**Q: DLT 645 电表的 BCD 码是什么意思？**

A: BCD（Binary-Coded Decimal）用 4 位二进制表示 1 位十进制数。例如 4 字节 BCD `12 34 56 78`（低字节在前发送）解码为 `123456.78`。通过 scale=0.01 转为实际 kWh。DLT 645 模块的 `BCDToDouble()` 自动处理此转换。

**Q: 双机冗余如何实际部署在两台物理机器上？**

A: 以两台机器 A（192.168.1.10）和 B（192.168.1.11）为例：
1. 两台机器安装相同的 PMPC 程序，使用相同的 `app_modules.ini` 和点表配置
2. A 机 `redundancy.ini` 中配置 `local_name=station_a`、`peer_ip=192.168.1.11`、`priority=200`
3. B 机 `redundancy.ini` 中配置 `local_name=station_b`、`peer_ip=192.168.1.10`、`priority=100`
4. 防火墙放行 TCP 7503（心跳）和 7502（同步）端口
5. 启动后 A 机检测到对端在线且自身优先级高 → 成为 Master 启动采集；B 机成为 Standby 待命
6. 可通过调试控制台 `role master`/`role standby` 手动切换角色验证
7. 可将 A 机 `data_recorder.ini` 的 `source=192.168.1.10`、B 机 `source=192.168.1.11`，共享同一個 MySQL 数据库，通过 `source` 字段区分数据来源

**Q: TCP 串口服务器如何配置？**

A: 串口服务器（如有人 USR-N510）将 RS-485/232 转为 TCP/IP。配置步骤：
1. 将串口服务器通过网线接入局域网，分配固定 IP（如 `192.168.1.200`）
2. 设置串口服务器的工作模式为 **TCP Server**，监听端口（如 `10000`）
3. 设置串口参数（波特率/校验位/数据位/停止位）与对端设备一致
4. PMPC 配置中使用 `port_name=192.168.1.200:10000`，PMPC 作为 TCP 客户端主动连接
5. 串口参数（baud/parity/data_bits/stop_bits）只需在 PMPC 配置中设定，串口服务器侧需与之匹配

支持混合组网：同一 PMPC 的不同通道可同时使用物理串口和 TCP 串口服务器。

**Q: 双机冗余模式下 MySQL 如何共享？**

A: 两台 PMPC 实例可共享同一 MySQL 数据库，通过 `data_recorder.ini` 中的 `source` 字段区分：
- 主机 A 配置 `source=192.168.1.10`
- 备机 B 配置 `source=192.168.1.11`
两台机器连接相同的 MySQL host/port/db。查询时按 `source` 过滤即可区分数据来源。注意：`rt_status` 表仅 Master 写入，Standby 写入会被主机数据覆盖。

**Q: CDT 的主站和从站区别？**

A: CDT 是循环式规约，从站(RTU)主动循环上送，主站被动接收。`cdt_slave` 模块定时读取本地四遥值发送 YC/YX/YM 帧，`cdt_master` 模块接收并解析。CDT 帧同步头为 `EB 90 EB 90 EB 90`。

**Q: 串口模块如何支持 TCP 串口服务器？**

A: 所有串口模块（modbus_rtu_master/slave、dlt645_master、cdt_master/slave、iec101_master/slave、iec103_master）使用 `CommIO` 统一 IO 封装。`port_name` 配置为 `COM1` 走物理串口，配置为 `192.168.1.100:10000`（含 `:` 且不含 `\`/`/`）自动走 TCP。无需修改程序逻辑。

**Q: MySQL 数据库需要手动建表吗？**

A: 不需要。`data_recorder` 模块启动时自动执行 `CREATE TABLE IF NOT EXISTS` 创建所有表（`rt_status`、`di_log_当前月`、`ai_log_当前月`、`pmpc_instance`、`schema_version`）。多台 PMPC 同时建表不会冲突，MySQL 保证 `IF NOT EXISTS` 原子性。

**Q: 如何查看存储到 MySQL 的数据？**

A: 使用任何 MySQL 客户端连接，直接查询：

```sql
SELECT * FROM rt_status WHERE source='192.168.1.100';
SELECT * FROM di_log_202606 ORDER BY ts_ms DESC LIMIT 50;
SELECT * FROM ai_log_202606 WHERE tag='AI_母线电压' ORDER BY ts_ms;
```

或其他语言（Python/Java/Web）通过 JDBC/ODBC 连接查询制作曲线、报表。

**Q: 两台 PMPC 连接同一数据库会冲突吗？**

A: 不会。每台配置不同的 `source` 字段（如 IP 地址），所有表通过 `source` 区分数据来源。`rt_status` 主键为 `(source, ch, dev, pt, type)`，日志表用自增 ID。互不影响。

**Q: AI 存盘的时间对齐是什么意思？**

A: 即 `aligned_ms = floor(时间戳 / 间隔毫秒) * 间隔毫秒`。如间隔 5 分钟（300000ms），12:03:45 → 对齐到 12:00:00。同一对齐窗口内多次数据只保留一条，便于曲线查询。

---

*文档版本 2.4 · 2026年6月*
